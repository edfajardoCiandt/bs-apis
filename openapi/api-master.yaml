apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  namespace: default
  name: api-master
  description: Master list of all APIs
  annotations:
    backstage.io/techdocs-ref: dir:.
spec:
  type: openapi
  lifecycle: experimental
  owner: team-c
  definition:
    $text: api-master_swagger.yaml
